{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:portals": "vite build && rm -rf frontend/dist admin-frontend/dist sub-vendor-frontend/dist && cp -r dist frontend/dist && cp -r dist admin-frontend/dist && cp -r dist sub-vendor-frontend/dist"
  },
  "dependencies": {
    "firebase": "^12.17.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "lucide-react": "^0.468.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
